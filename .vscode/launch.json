{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "node",
        "request": "attach",
        "name": "Attach to node in Docker",
        "port": 9229,
        "address": "localhost",
        "sourceMapPathOverrides": {
          "/usr/app/*": "${workspaceRoot}/*"
        }
      }
    ]
  }